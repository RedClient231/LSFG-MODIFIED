name: Android Build

on:
  push:
    branches: [ main, master ]
  pull_request:
  workflow_dispatch:

# Required so GITHUB_TOKEN can publish to the rolling 'latest-debug' release.
permissions:
  contents: write

jobs:
  build:
    name: Build LsfgDiag debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.11.1

      - name: Make gradlew executable
        run: chmod +x ./gradlew || true

      - name: Build debug APK
        # NDK + CMake are auto-installed by the Android SDK manager when the
        # AGP build resolves them. Default ubuntu-latest images already ship
        # NDK 26.x; if not, AGP fetches it on demand.
        run: gradle :lsfgdiag:assembleDebug --stacktrace

      - name: Rename APK with build number
        run: |
          mkdir -p out
          cp lsfgdiag/build/outputs/apk/debug/lsfgdiag-debug.apk \
             "out/lsfgdiag-debug-${{ github.run_number }}.apk"

      - name: Upload debug APK as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: lsfgdiag-debug-apk
          path: out/*.apk
          if-no-files-found: error
          retention-days: 30

      - name: Publish APK to a rolling "latest-debug" release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="latest-debug"
          gh release delete "$TAG" --repo "${{ github.repository }}" --yes --cleanup-tag 2>/dev/null || true
          gh release create "$TAG" \
            --repo "${{ github.repository }}" \
            --title "Latest debug build (build #${{ github.run_number }})" \
            --notes "Auto-published from commit ${{ github.sha }} on $(date -u +'%Y-%m-%d %H:%M UTC').

          Sideload \`lsfgdiag-debug-${{ github.run_number }}.apk\` on Android 13+." \
            "out/lsfgdiag-debug-${{ github.run_number }}.apk"
